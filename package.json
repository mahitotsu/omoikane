{
  "name": "omoikane-workspace",
  "version": "1.0.0",
  "description": "Omoikane Workspace - ITDelivery メタモデルとECサイト実例",
  "private": true,
  "workspaces": [
    "omoikane-metamodel",
    "omoikane-example-ecsite"
  ],
  "scripts": {
    "build": "bun run --filter='*' build",
    "dev": "bun run --filter='*' dev", 
    "type-check": "bun run --filter='*' type-check",
    "test": "bun run --filter='*' test",
    "install-all": "bun install"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.0.0"
  },
  "engines": {
    "bun": ">=1.0.0"
  }
}
