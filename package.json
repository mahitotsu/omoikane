{
  "name": "omoikane",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "generate-types": "bun run scripts/generate-typed-references.ts",
    "convert-refs": "bun run scripts/convert-to-typed-refs.ts",
    "migrate": "bun run generate-types && bun run convert-refs",
    "auto-build": "bun run scripts/auto-build.ts",
    "build": "bun run auto-build",
    "dev": "bun run auto-build && bun run examples/showcase.ts"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
